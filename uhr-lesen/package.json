{
  "name": "uhr-lesen",
  "private": true,
  "description": "Uhr lesen – German clock-reading app for children (static web app).",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node build.js",
    "serve": "python3 -m http.server 8000"
  }
}
